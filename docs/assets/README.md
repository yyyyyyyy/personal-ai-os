# README 截图资源

| 文件 | 说明 |
|------|------|
| `chat-approval.png` | 对话 + 高风险工具审批 |
| `goals.png` | 目标管理 |
| `export.png` | 一键无损导出 |

## 重新生成

```bash
make screenshots
```

源文件在 `mock/*.html`（静态 UI 示意，与真实前端风格一致）。修改 HTML 后重新运行上述命令即可更新 PNG。
